# Sugerencia para que la computadora juegue: Puede usar el método choice de la clase random

import numpy as np
# opciones posibles para escoger
opciones = ['piedra','papel','tijera']
# escoger una opción de forma aleatoria
opcionComputadora = np.random.choice(opciones)
# ver la opción seleccionada
opcionComputadora

# Si ejecuta varias veces esta celda verá como escoge opciones distintas

def definirganador(usuario, computadora)
    
    if usuario == computadora
        return "Empate"

    if ( usuario == "piedra" and computadora == "tijera"  ):
        return "Usuario gano"

    if ( usuario == "piedra" and computadora == "tijera"  ):
        return "Usuario gano"

    if ( usuario == "piedra" and computadora == "tijera"  ):
        return "Usuario gano"

    if ( usuario == "piedra" and computadora == "tijera"  ):
        return "Usuario gano"            